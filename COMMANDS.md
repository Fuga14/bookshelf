# Команди для роботи з проєктом Bookshelf

Цей файл містить список усіх необхідних команд для роботи з проєктом.

## 🚀 Початкове налаштування

### Встановлення залежностей

```bash
bundle install
```

### Налаштування бази даних

#### Варіант 1: Використання Docker (рекомендовано)

```bash
# Запустити PostgreSQL в Docker
docker-compose up -d db

# Перевірити статус контейнера
docker ps --filter "name=bookshelf_db"

# Переглянути логи бази даних
docker-compose logs db

# Зупинити базу даних
docker-compose down
```

#### Варіант 2: Локальна установка PostgreSQL

```bash
# Встановити PostgreSQL через Homebrew (macOS)
brew install postgresql@16

# Запустити PostgreSQL сервіс
brew services start postgresql@16

# Перевірити статус сервісу
brew services list | grep postgresql

# Зупинити PostgreSQL сервіс
brew services stop postgresql@16
```

## 📊 Робота з базою даних

### Створення бази даних

```bash
bin/rails db:create
```

### Виконання міграцій

```bash
# Виконати всі міграції
bin/rails db:migrate

# Перевірити статус міграцій
bin/rails db:migrate:status

# Відкотити останню міграцію
bin/rails db:rollback

# Відкотити N останніх міграцій
bin/rails db:rollback STEP=3
```

### Наповнення бази даних

```bash
# Завантажити seeds (початкові дані)
bin/rails db:seed

# Скинути базу даних (видалити, створити, міграції, seeds)
bin/rails db:reset

# Скинути базу даних для тестового середовища
bin/rails db:reset RAILS_ENV=test
```

### Перевірка бази даних

```bash
# Перевірити версію схеми бази даних
bin/rails db:version

# Відкрити консоль бази даних
bin/rails dbconsole

# Перевірити підключення до бази даних
bin/rails runner "ActiveRecord::Base.connection.execute('SELECT version()')"
```

### Створення міграцій

```bash
# Створити нову міграцію
bin/rails generate migration MigrationName

# Створити міграцію з додаванням колонки
bin/rails generate migration AddColumnNameToTableName column_name:type

# Створити міграцію з видаленням колонки
bin/rails generate migration RemoveColumnNameFromTableName column_name:type
```

## 🏃 Запуск додатку

### Запуск сервера розробки

```bash
# Запустити Rails сервер (стандартний спосіб)
bin/rails server
# або коротка версія
bin/rails s

# Запустити через bin/dev (з Turbo та іншими інструментами)
bin/dev

# Запустити на іншому порту
bin/rails server -p 3001
```

### Запуск консолі Rails

```bash
# Відкрити Rails console
bin/rails console
# або коротка версія
bin/rails c

# Виконати Ruby код безпосередньо
bin/rails runner "puts Book.count"
```

## 🧪 Тестування

### Запуск тестів

```bash
# Запустити всі тести
bin/rails test

# Запустити конкретний тест
bin/rails test test/models/book_test.rb

# Запустити тести з детальним виводом
bin/rails test --verbose
```

## 🔧 Генерація коду

### Генерація моделей

```bash
# Створити модель з міграцією
bin/rails generate model ModelName field1:type field2:type

# Приклад: Створити модель Book
bin/rails generate model Book title:string author:string year:integer description:text
```

### Генерація контролерів

```bash
# Створити контролер з діями
bin/rails generate controller ControllerName action1 action2

# Приклад: Створити BooksController
bin/rails generate controller Books index show new edit create update destroy
```

### Генерація міграцій

```bash
# Створити міграцію для створення таблиці
bin/rails generate migration CreateTableName

# Створити міграцію для додавання колонок
bin/rails generate migration AddFieldsToTableName field1:type field2:type
```

## 📋 Перевірка маршрутів

### Перегляд маршрутів

```bash
# Показати всі маршрути
bin/rails routes

# Показати маршрути для конкретного ресурсу
bin/rails routes | grep books

# Показати маршрути в табличному форматі
bin/rails routes --expanded
```

## 🧹 Очищення та обслуговування

### Очищення кешу

```bash
# Очистити кеш
bin/rails tmp:clear

# Очистити лог файли
bin/rails log:clear
```

### Перезапуск додатку

```bash
# Перезапустити сервер (Ctrl+C для зупинки, потім знову запустити)
# Або використати Spring для швидшого перезапуску
bin/spring stop
```

## 🔍 Корисні команди для розробки

### Перевірка версії Ruby та Rails

```bash
# Версія Ruby
ruby -v

# Версія Rails
bin/rails -v

# Версія Bundler
bundle -v
```

### Перевірка залежностей

```bash
# Перевірити встановлені gems
bundle list

# Оновити gems
bundle update

# Перевірити застарілі gems
bundle outdated
```

### Робота з Git (якщо використовується)

```bash
# Перевірити статус
git status

# Додати файли
git add .

# Зробити commit
git commit -m "Опис змін"

# Показати історію
git log --oneline
```

## 🐳 Docker команди

### Робота з Docker Compose

```bash
# Запустити всі сервіси
docker-compose up

# Запустити в фоновому режимі
docker-compose up -d

# Зупинити сервіси
docker-compose down

# Переглянути логи
docker-compose logs

# Переглянути логи конкретного сервісу
docker-compose logs db

# Перезапустити сервіси
docker-compose restart
```

### Робота з Docker контейнерами

```bash
# Показати запущені контейнери
docker ps

# Показати всі контейнери
docker ps -a

# Показати контейнери з фільтром
docker ps --filter "name=bookshelf"

# Зупинити контейнер
docker stop bookshelf_db

# Видалити контейнер
docker rm bookshelf_db

# Видалити всі зупинені контейнери
docker container prune
```

## 📝 Приклади роботи з моделлю Book

### У Rails консолі

```ruby
# Відкрити консоль
bin/rails console

# Приклади команд в консолі:
Book.all                    # Отримати всі книги
Book.count                  # Підрахувати кількість книг
Book.first                  # Отримати першу книгу
Book.find(1)                # Знайти книгу за ID
Book.where(author: "Author Name")  # Знайти книги за автором
Book.create(title: "Title", author: "Author", year: 2020, description: "Desc")
book = Book.new(...)        # Створити нову книгу
book.save                   # Зберегти книгу
book.update(title: "New Title")  # Оновити книгу
book.destroy                # Видалити книгу
```

## 🛠️ Додаткові інструменти

### Rubocop (перевірка стилю коду)

```bash
# Перевірити код
bin/rubocop

# Автоматично виправити помилки
bin/rubocop -a
```

### Brakeman (перевірка безпеки)

```bash
# Запустити перевірку безпеки
bin/brakeman
```

## 📚 Корисні посилання

- [Rails Guides](https://guides.rubyonrails.org/)
- [Rails API Documentation](https://api.rubyonrails.org/)
- [Bootstrap 5 Documentation](https://getbootstrap.com/docs/5.3/)
- [AppStack Theme](https://appstack.bootlab.io/)

---

**Примітка:** Всі команди виконуються з кореневої директорії проєкту (`/Users/vladyslavp/Documents/maga/bookshelf`).
